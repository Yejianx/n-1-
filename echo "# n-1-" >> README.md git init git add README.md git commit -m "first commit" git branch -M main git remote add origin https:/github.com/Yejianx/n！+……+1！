#define _CRT_SECURE_NO_WARNINGS 1

#include <stdio.h>

#define uint unsigned int


int main()
{
	int i = 0, j = 0;
	int num = 1, sum = 0;
    
	for (i = 1; i <= 4;i++)
	{
		num = num * i;

		sum = sum + num;
	}

	printf("%d",sum);
	


	return 0;
}
